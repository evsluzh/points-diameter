Simple visualizer of the points diameter algorithm.

Make project: 
    git submodule update &&
    cd visualization &&
    qmake && make
    cd ..
    qmake && make

Add point:               double click
Build points diameter:   press key 'Enter'
Save points:             press key 'S'
Load points:             press key 'L'
